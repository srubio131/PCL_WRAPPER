%nspace pcl::Point_types;

%include <stdint.i>

%{
#include <pcl/point_types.h>
%}


namespace pcl
{
	struct RGB
	{
		RGB ();
	};
	
	struct Intensity
	{
		Intensity();
	};
	
	struct Intensity8u
	{
		Intensity8u();
	};
	
	struct Intensity32u
	{
		Intensity32u();
	};
	
	struct PointXYZ
	{
		PointXYZ ();
		PointXYZ (float _x, float _y, float _z);
		float x, y, z;
		float data[4];
	};
	
	/*struct PointXYZRGBA
	{
		PointXYZRGBA ();
		PointXYZRGBA (float _x, float _y, float _z, uint32_t _rgba);
		float x, y, z;
		uint32_t rgba;
	};
	
	struct PointXYZRGB
	{
		PointXYZRGB ();
		PointXYZRGB (float _x, float _y, float _z, float _rgb);
		float x, y, z, rgb;
	};
	
	struct PointXYZRGBL
	{
		PointXYZRGBL ();
		PointXYZRGBL (float _x, float _y, float _z, uint32_t _rgba, uint32_t label);
		float x, y, z;
		uint32_t rgba, label;
	};
	
	struct PointXYZHSV
	{
		PointXYZHSV ();
		PointXYZHSV (float _x, float _y, float _z, float _h, float _s, float _v);
		float x, y, z, h, s, v;
	};
	
	struct PointXY
	{
		PointXY ();
		PointXY (float _x, float _y);
		float x, y;
	};*/
}
